Running App Indicators
======================

A small dot appears below (horizontal docks) or beside (vertical docks) each app icon that is currently running. This matches the native macOS Dock behavior.

The indicators update in real time as apps launch and quit — no polling required.
